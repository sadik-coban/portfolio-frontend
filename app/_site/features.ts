// Feature flags for UI bits that may or may not ship. Flip a flag to false to
// remove the feature entirely (no button, no behavior) — handy while deciding
// whether to deploy it. Fully reversible.

/** Desktop icon-collapse toggle on the car-price project sidebar (FinalShell). */
export const SIDEBAR_COLLAPSE_ENABLED = true;
